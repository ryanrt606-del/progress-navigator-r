import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.progress.navigator',
  appName: 'Progress Navigator',
  webDir: 'dist'
};

export default config;
